@page
@model zoo.Pages_EventsZoo.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EventModel[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EventModel[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EventModel[0].ETime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.EventModel) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ETime)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.EventModelId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.EventModelId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.EventModelId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
